install pandas and bs4 for python

pip install python
pip install beautifulsoup4

(will create a file that does all this automatically if needed just create an issue on jira and assign it to Rahul)

download the data from kaggle using the link given below 
https://www.kaggle.com/hernan4444/anime-recommendation-database-2020

imgae_scrape_main.py is the file that is to be run
    (sorry for not having relative links) set the path for anime.csv from the data 
    also set the path for the folder that will save the images 